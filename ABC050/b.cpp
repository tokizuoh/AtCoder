#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(int i = 0; i < n; i++)

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    int n;
    cin >> n;
    vector<int> T(n);
    REP(i, n) cin >> T[i];

    int m;
    cin >> m;
    vector<int> P(m), X(m);
    REP(i, m) cin >> P[i] >> X[i];

    

    return 0;
}