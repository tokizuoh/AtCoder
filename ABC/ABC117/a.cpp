#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    double t, x;
    cin >> t >> x;

    cout << fixed << setprecision(8) << (t / x) << endl;

    return 0;
}