#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    long long n;
    vector<long long> A(5);
    cin >> n;
    for(int i = 0; i < 5; i++){
        cin >> A[i];
    }

    long long ans = 0;
    for(int i = 0; i < n; i++){
    
    }



    return 0;
}