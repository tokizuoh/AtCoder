#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int x, a;
    cin >> x >> a;

    if(x < a){
        cout << 0 << endl;
    }else{
        cout << 10 << endl;
    }

    return 0;
}